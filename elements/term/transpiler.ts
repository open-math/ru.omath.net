import { defineAccentTranspiler } from '@erudit-js/bitran-elements/accent/transpiler';

import { termName } from './shared';

export const termTranspiler = defineAccentTranspiler({
    objName: termName,
});
